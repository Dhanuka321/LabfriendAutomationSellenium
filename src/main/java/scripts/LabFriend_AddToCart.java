package scripts;

import java.io.IOException;

import org.testng.Assert;
import org.testng.annotations.Test;

import com.aventstack.extentreports.ExtentReports;

import functions.Labfriend_BaseClassFun;

/**
 * @author Dhanuka_Dulanjana
 * 
 */

public class LabFriend_AddToCart extends Labfriend_BaseClassFun {

	@Test
	public void labfriendAddToCart() throws IOException {
		
		
		
		//launchApplication();
		//reportConfig();
//		ExtentReports extent=reportConfig();
//		extent.createTest("TestName");
	//	launchApplication(); @BeforeMethod Annotation Can be used for this instead of calling here
//		Actions action = new Actions(driver);
//		action.moveToElement(driver.findElement(By.xpath("//input[@name='q'][1]"))).click().build().perform();
//		action.moveToElement(driver.findElement(By.xpath("//input[@name='q'][1]"))).sendKeys("pip").build().perform();

	}
	

	@Test
	public void labfriendAddToCart_2() throws IOException {
		
		Assert.assertFalse(true);
		
		//launchApplication();
		//reportConfig();
//		ExtentReports extent=reportConfig();
//		extent.createTest("TestName");
	//	launchApplication(); @BeforeMethod Annotation Can be used for this instead of calling here
//		Actions action = new Actions(driver);
//		action.moveToElement(driver.findElement(By.xpath("//input[@name='q'][1]"))).click().build().perform();
//		action.moveToElement(driver.findElement(By.xpath("//input[@name='q'][1]"))).sendKeys("pip").build().perform();

	}

}
